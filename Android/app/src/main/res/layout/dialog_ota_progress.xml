<?xml version="1.0" encoding="utf-8"?>
<!--
    Layout del dialog de progreso del OTA. Mostramos:
      - ProgressBar horizontal (0-100) actualizada por OtaUpdateTask.onProgressUpdate
      - TextView con el status text actual ("Descargando 40%…")
      - TextView con la version target (info contextual)

    Se infla en DeviceDetailActivity.showOtaProgressDialog() y se referencia
    por findViewById desde dentro del AsyncTask.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="24dp"
    android:paddingEnd="24dp"
    android:paddingTop="16dp"
    android:paddingBottom="8dp">

    <TextView
        android:id="@+id/ota_target_version"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="14sp"
        android:textColor="?android:attr/textColorSecondary"
        android:layout_marginBottom="20dp"
        tools:text="Actualizando a v1.1.0"
        xmlns:tools="http://schemas.android.com/tools" />

    <ProgressBar
        android:id="@+id/ota_progress_bar"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:max="100"
        android:progress="0"
        android:indeterminate="false" />

    <TextView
        android:id="@+id/ota_status_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="14sp"
        android:textColor="?android:attr/textColorPrimary"
        android:layout_marginTop="12dp"
        android:layout_marginBottom="8dp"
        xmlns:tools="http://schemas.android.com/tools"
        tools:text="Descargando 40%…" />

</LinearLayout>
